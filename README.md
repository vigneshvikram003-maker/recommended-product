# recommended-product
This project demonstrates the implementation of the K-Nearest Neighbors (KNN) algorithm using a small manually created dataset and The KNN model analyzes the similarity between the data points to classify and recommend a product category.
